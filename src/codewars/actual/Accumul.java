package codewars.actual;

public class Accumul {

    public static String accum(String s) {
        String result = new String();

        for (int i = 0; i < s.length(); i++ )

            for (int j = 0; j < 10; j++);{

        }

                s.substring(1,5);

            char mychar = 3;
            char mychear = 4;

        char sfc = Character.highSurrogate(mychar + mychear);
        System.out.println(sfc);
        return result ;
    }

    public static void main(String[] args) {
        String str = "RafałBorowik";
        System.out.println(accum(str));
    }
}
